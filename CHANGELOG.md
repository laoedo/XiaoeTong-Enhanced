# 更新日志

本项目遵循语义化版本号。

## 3.0.10 - 2026-09-02

- 将悬浮按钮触发方式改为固定坐标热区：鼠标靠近按钮周围或右侧滚动条时即可展开，避免滑动容器自身触发悬停造成反复收放和抖动。

## 3.0.9 - 2026-09-02

- 定位与设置按钮默认进一步收进浏览器右侧边缘，仅保留约 `10px` 的悬停触发区域。

## 3.0.8 - 2026-09-02

- 设置按钮改用与定位按钮相同的 `34px` 白色 SVG 图标，两枚悬浮按钮的视觉尺寸保持一致。

## 3.0.7 - 2026-09-02

- 将自动连播目录稳定逻辑移动到小鹅通最终的 `reloadSwitch` 重建入口：学习进度照常提交，只阻止目录组件重建。
- 定位与设置按钮默认收进浏览器右侧边缘，仅保留可悬停的小边，鼠标移入或键盘聚焦时自动滑出。
- 定位按钮改用更大的白色准星图标，提高辨识度。

## 3.0.6 - 2026-09-02

- 自动连播接近结束时不再触发课程目录整体刷新，学习进度上报和当前课程状态仍保留。
- 右侧课程栏适当加宽，课程目录标题保持单行，章节内容更贴近滚动条以减少空白。
- 设置按钮上方新增“定位当前播放小节”按钮，可将当前课程平滑滚动到目录中央并短暂高亮。

## 3.0.5 - 2026-09-02

- 播放器“下一集”按钮和自动连播改为复用无刷新切课流程，不再整页重新加载。
- 双栏布局左右边距和栏间距按 YouTube 风格收紧，扩大实际内容区域。
- 课程目录改用更宽、更清晰的滚动条，并分别适配浅色和深色模式。
- 设置面板新增大章节与小节字号滑块，可分别实时调节并自动保存。

## 3.0.4 - 2026-09-02

- “上次学习”改为跟随刚刚离开的课程，不再固定停留在页面初始课程。
- 隐藏小鹅通原生的重叠标签，将脚本标签放到课程项最右侧并预留文字空间。
- 设置面板新增“章节彩色分组”开关，默认开启并可随时关闭。
- 各大章节采用低饱和度、冷暖交替的独立色系，所属小节沿用同一色系。
- 当前播放和鼠标悬停继续使用中性灰色，避免与章节底色混淆。

## 3.0.3 - 2026-09-02

- 修复展开章节仍受 `576px` 高度限制、31 节课程只显示前 8 节的问题。
- 展开的章节改为完整高度，统一使用外层课程目录滚动，避免嵌套滚动和内容裁切。
- 移除“上次学习”课程残留的边框，只保留文字提示。
- 大课程切换区隐藏下一张卡片的残余边缘，并将左右切换按钮改为更清晰的大箭头。
- 课程项之间增加细分割线，不额外增加行距，使目录层次更清晰。

## 3.0.2 - 2026-09-02

- 修复深色模式下课程项悬停时被小鹅通原样式覆盖成白色背景的问题。
- 只保留当前播放课程的选中背景，“上次学习”记录不再显示为第二个选中项。
- 当前播放课程增加播放三角图标，并移除目录项左侧的蓝色强调线。
- 当前项和悬停项使用不同深度的灰色背景，状态区分更清晰。

## 3.0.1 - 2026-09-02

- 将视频切换改为仅销毁并重建左侧播放器，右侧目录和页面主体不再重新渲染。
- 保留切课前的目录滚动位置，并同步更新网址、播放器数据、字幕、线路和学习进度。
- 在播放器下方新增当前课程名称。
- 修复大课程标签、相关推荐等组件在深色模式下仍显示白底的问题。
- 增强课程悬停和当前课程的背景、边框及左侧高亮对比度。

## 3.0.0 - 2026-09-02

- 新增 YouTube 式视频学习布局：左侧大播放器、右侧独立滚动课程目录。
- 新增视频课程无刷新切换，并支持浏览器后退和前进。
- 无刷新切课复用小鹅通原生 Vue 组件生命周期、权限检查和进度读取；失败时自动退回普通跳转。
- 深色模式中的课程标题、章节、状态和提示文字统一调整为白色。
- 新增切课加载、课程悬停和界面过渡动画，并支持系统减少动态效果设置。
- 设置面板新增学习布局、无刷新切课和界面动画开关。
- 保留并迁移 2.x 版本已经保存的用户设置。

## 2.0.0 - 2026-09-02

- 新增页面设置悬浮按钮和独立配置面板。
- 新增浅色、深色页面配色切换。
- 将已解锁且可访问的课程标题增强为真实链接，支持浏览器原生的新标签页操作。
- 为防刷新功能新增开关，并在变更后提示刷新生效。
- 监听小鹅通 SPA 页面变化，在课程目录更新后自动重新应用增强。

## 1.0.0 - 2026-08-22

- 首次公开版本。
- 将页面可见状态保持为可见。
- 拦截标签可见性和窗口焦点变化事件。
- 限定在小鹅通 PC 课程详情页运行。
- 添加中英文脚本元数据和内嵌图标。
